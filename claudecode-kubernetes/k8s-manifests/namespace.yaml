# claudecode-terminal namespace definition
# Isolated namespace where the web terminal service and user sandbox Pods run
apiVersion: v1
kind: Namespace
metadata:
  name: claudecode-terminal
  labels:
    app.kubernetes.io/part-of: claudecode-web-terminal
